# file-funeral セットアップガイド

このガイドでは、file-funeralのインストールから初回セットアップまでを詳しく説明します。

## 目次

- [システム要件](#システム要件)
- [インストール](#インストール)
  - [ソースからビルド](#ソースからビルド)
  - [ビルド済みバイナリ](#ビルド済みバイナリ-準備中)
- [AWS S3の準備](#aws-s3の準備)
  - [S3バケットの作成](#1-s3バケットの作成)
  - [IAMユーザーの作成と認証情報の取得](#2-iamユーザーの作成と認証情報の取得)
- [アプリケーションの初期設定](#アプリケーションの初期設定)
  - [認証情報の設定](#1-認証情報の設定)
  - [同期フォルダの選択](#2-同期フォルダの選択)
  - [接続テスト](#3-接続テスト)
- [初回同期](#初回同期)
- [自動同期の設定](#自動同期の設定)
- [次のステップ](#次のステップ)

---

## システム要件

### 必須環境

- **オペレーティングシステム**:
  - macOS 11.0 (Big Sur) 以降
  - Windows 10以降（将来対応予定）
  - Linux（Ubuntu 20.04以降、将来対応予定）

- **開発環境（ソースからビルドする場合）**:
  - Rust 1.92.0以降
  - Node.js 18以降
  - npm または pnpm

### 推奨スペック

- RAM: 4GB以上
- ディスク空き容量: 500MB以上（アプリ本体 + 同期するファイル）
- インターネット接続: 常時接続（同期時に必要）

---

## インストール

### ソースからビルド

#### 1. リポジトリのクローン

```bash
git clone https://github.com/ohr486/file-funeral.git
cd file-funeral
```

#### 2. 依存関係のインストール

```bash
# Node.js依存関係をインストール
npm install

# Tauri CLIをグローバルにインストール（推奨）
cargo install tauri-cli
```

#### 3. 開発モードで実行

```bash
# 開発サーバーを起動（初回は3-5分かかります）
npm run tauri:dev
```

初回起動時はRustの依存関係のダウンロードとコンパイルに時間がかかります。

#### 4. 本番ビルド（オプション）

```bash
# デスクトップアプリケーションとしてパッケージ化
npm run tauri:build
```

ビルドが成功すると、以下のディレクトリに実行ファイルが生成されます:
- macOS: `src-tauri/target/release/bundle/macos/`
- Windows: `src-tauri/target/release/bundle/msi/`
- Linux: `src-tauri/target/release/bundle/deb/` または `appimage/`

### ビルド済みバイナリ（準備中）

将来のリリースでは、GitHubのReleasesページからビルド済みのバイナリをダウンロードできるようになります。

---

## AWS S3の準備

file-funeralを使用するには、AWS S3バケットとアクセス認証情報が必要です。

### 1. S3バケットの作成

1. [AWS Management Console](https://console.aws.amazon.com/)にサインイン
2. S3サービスに移動
3. 「バケットを作成」をクリック
4. バケット設定:
   - **バケット名**: 一意の名前を入力（例: `my-file-funeral-backup`）
   - **リージョン**: お住まいの地域に近いリージョンを選択
     - 日本: `ap-northeast-1` (東京)
     - 米国東部: `us-east-1` (バージニア北部)
     - 米国西部: `us-west-2` (オレゴン)
   - **ブロックパブリックアクセス**: デフォルト設定（すべてブロック）を推奨
   - **バージョニング**: 有効化を推奨（ファイル履歴管理）
   - **暗号化**: デフォルト暗号化を有効化（SSE-S3）を推奨
5. 「バケットを作成」をクリック

### 2. IAMユーザーの作成と認証情報の取得

#### 2.1 IAMユーザーの作成

1. AWS Management ConsoleでIAM（Identity and Access Management）に移動
2. 左側のメニューから「ユーザー」を選択
3. 「ユーザーを作成」をクリック
4. ユーザー名を入力（例: `file-funeral-user`）
5. 「次へ」をクリック

#### 2.2 権限の設定

以下の2つの方法から選択してください:

**方法1: 既存ポリシーをアタッチ（簡単）**

1. 「ポリシーを直接アタッチ」を選択
2. `AmazonS3FullAccess` を検索して選択
3. 「次へ」→ 「ユーザーを作成」

**方法2: カスタムポリシー（推奨・最小権限）**

1. 「ポリシーを直接アタッチ」を選択
2. 「ポリシーを作成」をクリック
3. JSON エディタで以下のポリシーを入力:

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": [
        "s3:ListBucket",
        "s3:GetBucketLocation"
      ],
      "Resource": "arn:aws:s3:::YOUR-BUCKET-NAME"
    },
    {
      "Effect": "Allow",
      "Action": [
        "s3:PutObject",
        "s3:GetObject",
        "s3:DeleteObject",
        "s3:GetObjectAttributes"
      ],
      "Resource": "arn:aws:s3:::YOUR-BUCKET-NAME/*"
    }
  ]
}
```

**重要**: `YOUR-BUCKET-NAME` を実際のバケット名に置き換えてください。

4. 「次へ」をクリック
5. ポリシー名を入力（例: `FileFuneralS3Access`）
6. 「ポリシーを作成」をクリック
7. ユーザー作成画面に戻り、作成したポリシーを選択
8. 「次へ」→ 「ユーザーを作成」

#### 2.3 アクセスキーの生成

1. 作成したユーザーをクリック
2. 「セキュリティ認証情報」タブを選択
3. 「アクセスキー」セクションで「アクセスキーを作成」をクリック
4. 「ローカルコード」を選択
5. 確認チェックボックスにチェックを入れ、「次へ」をクリック
6. 説明タグを入力（例: `file-funeral-app`）
7. 「アクセスキーを作成」をクリック
8. **重要**: 以下の情報をメモしてください:
   - **アクセスキーID** (例: `AKIAIOSFODNN7EXAMPLE`)
   - **シークレットアクセスキー** (例: `wJalrXUtnFEMI/K7MDENG/...`)

**⚠️ 警告**: シークレットアクセスキーはこの画面でしか表示されません。安全な場所に保管してください。

---

## アプリケーションの初期設定

### 1. 認証情報の設定

1. file-funeralアプリを起動:
   ```bash
   npm run tauri:dev
   ```

2. 初回起動時、セットアップウィザードが表示されます（または右上の「Settings」ボタンをクリック）

3. AWS認証情報を入力:
   - **Access Key ID**: 先ほどメモしたアクセスキーID
   - **Secret Access Key**: シークレットアクセスキー
   - **Region**: S3バケットを作成したリージョン（例: `ap-northeast-1`）
   - **S3 Bucket Name**: 作成したバケット名

4. 「Save Credentials」ボタンをクリック

**セキュリティ**: 認証情報はmacOSキーチェーンに暗号化されて保存されます。平文では保存されません。

### 2. 同期フォルダの選択

1. 「Select Folder」ボタンをクリック
2. 同期したいローカルフォルダを選択
3. （オプション）Remote Prefixを設定:
   - 空欄: S3バケットのルートに同期
   - 例: `documents/` - S3バケット内の `documents/` フォルダに同期

### 3. 接続テスト

1. 「Test Connection」ボタンをクリック
2. 成功すると「Connected successfully」と表示されます
3. エラーが表示される場合は、[トラブルシューティング](./TROUBLESHOOTING.md)を参照してください

---

## 初回同期

初回同期では、ローカルとクラウドのすべてのファイルを比較し、同期します。

### 同期前の確認

1. メイン画面でファイル一覧を確認
2. 各ファイルの同期状態を確認:
   - 🟢 **In Sync**: 同期済み
   - 🔵 **Needs Upload**: アップロード必要
   - 🟡 **Needs Download**: ダウンロード必要
   - 🔴 **Conflict**: 競合あり（両方保存されます）

### 同期の実行

1. 「Sync」ボタンをクリック
2. 同期プロセスが開始されます:
   - プログレスバーで進捗を確認
   - ログで詳細を確認
3. 完了すると、同期結果が表示されます:
   - アップロード数
   - ダウンロード数
   - 競合解決数
   - 削除数

### 競合の扱い

ファイルがローカルとクラウドの両方で変更されている場合:
- **クラウド版**: 元のファイル名で保存
- **ローカル版**: `filename (PC名's conflicted copy YYYY-MM-DD).ext` として保存
- 両方のファイルが保持され、ユーザーが後で統合できます

---

## 自動同期の設定

file-funeralは自動同期機能を提供します。

### 設定方法

1. Settings画面を開く
2. Auto Syncセクションで以下を設定:
   - ✅ **Sync on startup**: アプリ起動時に自動同期
   - ✅ **Sync on shutdown**: アプリ終了時に自動同期（推奨）
3. 「Save Settings」をクリック

### 推奨設定

- **起動時同期**: 有効 - 他のPCでの変更を取得
- **終了時同期**: 有効 - 作業内容を確実にバックアップ

---

## 次のステップ

セットアップが完了しました！次のドキュメントも参照してください:

- [TROUBLESHOOTING.md](./TROUBLESHOOTING.md) - 問題が発生した場合
- [README.md](./README.md) - 使い方の詳細
- [ARCHITECTURE.md](./ARCHITECTURE.md) - システムの仕組み（開発者向け）

### よくある質問

**Q: 複数のフォルダを同期できますか？**
A: v1.0では1つのフォルダのみサポートしています。v2.0で複数フォルダ対応予定です。

**Q: Google Drive や Dropbox には対応していますか？**
A: v1.0ではAWS S3のみ対応しています。将来的にGoogle Cloud Storage（v2.5）やS3互換ストレージ（v2.0）をサポート予定です。

**Q: 暗号化されますか？**
A: S3のサーバーサイド暗号化（SSE-S3）を使用しています。クライアントサイド暗号化はv3.0で計画中です。

**Q: 削除したファイルを復元できますか？**
A: v1.0では削除は完全同期されます。ソフト削除（ゴミ箱機能）はv2.0で計画中です。S3のバージョニングを有効にしている場合は、AWS Consoleから復元できます。

**Q: どれくらいの料金がかかりますか？**
A: AWS S3の料金はストレージ量とデータ転送量によります。詳細は[AWS S3料金ページ](https://aws.amazon.com/s3/pricing/)を参照してください。通常の個人利用では月数ドル程度です。

---

問題が発生した場合は、[GitHubのIssues](https://github.com/ohr486/file-funeral/issues)で報告してください。
